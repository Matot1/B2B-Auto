---
description: Never add Cursor as git commit co-author
alwaysApply: true
---

# No Co-authored-by

Never add this line to any commit:

`Co-authored-by: Cursor <cursoragent@cursor.com>`

Do not pass `--trailer`, do not put that text in the commit message. Use `git commit-tree` if `git commit` injects it.
